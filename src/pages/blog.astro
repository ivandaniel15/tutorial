---
import BaseLayout from '../layouts/BaseLayout.astro';
import BlogPostTile from '../components/BlogPostTile.astro';
const pageTitle = "Welcome to my Astro Blog!";

const allPosts = Object.values(
  import.meta.glob('../pages/posts/*.md', { eager: true })
);
---
<BaseLayout pageTitle={pageTitle}>
	<p>This is where I will post about my journey learning Astro.</p>
	<ul>
		<li><a href="/posts/post-1">Post 1</a></li>
		<li><a href="/posts/post-2">Post 2</a></li>
		<li><a href="/posts/post-3">Post 3</a></li>
		<p>The automatic comes bellow:</p>
		
		
		<!-- 
		Isto era sem componente

		{allPosts.map((post: any) => (
			<li><a href={post.url}>{post.frontmatter.title}</a></li>
		))} -->
		
		{allPosts.map((post: any) => <BlogPostTile url={post.url} title={post.frontmatter.title} />)}
		 
	</ul>
	
</BaseLayout>
